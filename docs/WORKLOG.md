# ULSOO Heritage AI — 진행 로그

## 2026-07-07

### 목표
- GitHub 저장소(KIMHeeKwon/ULSOO-Heritage-AI) 로컬 구축 및 현행 index.html 분석
- Ref/ 연구자료(CHIC·MUCH 최종보고서, ULSOO 계획서) 기반 홈페이지 전면 개편 구상·설계

### 결정사항
- **호스팅: GitHub Pages 채택** (무료·빌드 불필요·현행 단일 HTML 즉시 서비스). 트래픽 증가 시 Cloudflare Pages 이전 예비안
- **디자인 폼 유지, 콘텐츠 전면 교체**: 다크+전통색 팔레트·CDN 폴백 구조는 검증된 강점으로 보존
- **IA 6섹션 재편**: Hero(KPI 4종 카운트업) / 연구 여정(신규, CHIC→MUCH→ULSOO 타임라인) / 핵심기술(3대 분야: Heritage AI · 디지털 생성복원 · 데이터 아키텍처) / 실증사례(6건) / 연구성과(대시보드형, 분야별 집계+대표성과만) / 연구진(4인: 이재호·김희권·박찬우·백서현)
- Ref/ 폴더는 .gitignore 처리 (원문 비공개)

### 산출물
- `.gitignore` (Ref/ 제외)
- 3개 보고서(총 259p) 분석 요약 — CHIC: 애셋 31,022개·사업화 23건 200억 규모·COLING 2022·장관표창 / MUCH: 4계층 데이터 패브릭·TTA 표준 TTAK.KO-10.1621·ICCV 2025·AVICOM 우수상·5대 시범서비스 / ULSOO: 10억픽셀·멀티모달·SSIM 95% 목표·국가유산청
- 설계 문서 `ulsoo-site-blueprint.html` (스크래치패드, 미리보기 패널 표시) — IA·와이어프레임·데이터 정정 매핑·시각자산 계획·4단계 로드맵
- PDF 이미지 인벤토리: 총 893개(고해상도 657개) 추출 가능 확인

### 현재 진행도
- 분석·설계 완료. 구현 착수 전 단계

### 남은 미해결 (사용자 확인 필요)
1. 박찬우·백서현 직급/역할/전문분야, 4인 캐리커처 제작 방향
2. ULSOO 과제 공식 표기(과제명·기간·선정 상태)
3. 보고서 내 이미지 웹 공개 가능 범위(저작권)
4. 이메일 공개 범위, 커스텀 도메인 여부

### 다음 단계
- Phase 1: data.js 분리 + 검증 수치로 데이터 교체 (정정 매핑 반영)
- Phase 2: 연구 여정 타임라인·성과 대시보드·연구진 4인 카드 구현
- Phase 3: 이미지 추출·큐레이션·WebP 변환
- Phase 4: GitHub Pages 배포 및 QA

### 추가 산출물 (같은 날, 2차 작업)
- **`docs/IMPLEMENTATION_SPEC.md` 작성** — 하위 모델/구현 담당자가 추가 질문 없이 개편을 수행할 수 있는 구현 명세서 v1.0
  - §0 절대 규칙(Ref/ 커밋 금지, 수치 변경 금지, CVPR "게재" 표기 금지, 폴백 보존)
  - §3 데이터 계약: `assets/data.js` 전문 — 6섹션 전체의 실제 카피·검증 수치 포함 (TODO_CONFIRM 처리 규칙 포함)
  - §4 섹션별 구현 명세: KPI 카운트업(코드 포함), 연구 여정 타임라인, 기술 아코디언, 성과 대시보드, 연구진 4인 카드(이미지 폴백 포함), §5 라이트박스
  - §6 추가 CSS 전문, §7 CDN 폴백 동기화 규칙, §8 이미지 파이프라인(명령 포함), §9 배포 절차, §10 완료 기준 체크리스트, §11 Phase별 작업 순서
- **구현은 2026-07-08 진행 예정** — 구현 세션은 IMPLEMENTATION_SPEC.md를 기준 문서로 삼을 것

### 추가 산출물 (같은 날, 3차 작업)
- **`docs/CONTENT_SPEC.md` 작성** — 화면 문안·사진 배치 명세서 v1.0
  - 전 섹션의 표시 문안 전문 (메타태그·네비·Hero·여정 3패널·실증 6카드·성과 집계·연구진 4카드·푸터·토스트)
  - **이미지 마스터 목록**: 슬롯 11종의 파일명·사용 위치·소스(PDF·페이지 특정)·비율·우선순위(P1~P3)
    - 실증 6카드(P1): MUCH p.61/65, CHIC p.78/91 등 페이지 단위 소스 지정 + 대안 소스
    - 예비 풀: CHIC p.58/59/69/71/87, MUCH p.22/46/54/58/72, ULSOO p.36–42
  - 연구진 캐리커처 제작 가이드(800×800, 4인 팔레트 지정), 어조·표기 규칙(금지 표현 포함)
- IMPLEMENTATION_SPEC.md 개정: 문서 우선순위(문안=CONTENT_SPEC, 구조·코드=IMPLEMENTATION_SPEC) 및 techFields 선택 이미지 렌더 규칙 반영

### 추가 산출물 (같은 날, 4차 작업) — 연구진 정보 확정 반영
- **목표**: 사용자 제공 연구진 정보(이메일 4건·박찬우/백서현 전문분야)를 두 명세서에 반영
- **결정사항**:
  - 이메일 확정: 이재호 jhlee3@ / 김희권 hkkim79@ / 박찬우 gamer@ / 백서현 b_seohy@ (모두 etri.re.kr)
  - 태그 확정: 박찬우 `응용 시스템`·`디지털 문화유산 생성` / 백서현 `문화보존과학`·`AI 기반 문화유산 복원·생성`
  - 박찬우·백서현 직급만 TODO_CONFIRM 유지 (표시값 "연구원")
  - **프로필 이미지는 추후 추가** — 준비 전까지 4인 전원 이니셜 아바타 폴백, member 이미지 우선순위 P2→P3 하향
- **산출물**: CONTENT_SPEC.md §7·§8 갱신, IMPLEMENTATION_SPEC.md §3 members 갱신
- **남은 미해결**: 박찬우·백서현 직급, 프로필 이미지(사진 vs 캐리커처) 방향
- **다음 단계**: Phase 1 구현 (data.js 분리 + 데이터 교체)

### 추가 산출물 (같은 날, 5차 작업) — 인터랙션·모션 디자인 반영
- **목표**: 각 섹션 성격에 맞는 인터랙션 디자인을 설계해 명세에 반영 (라이브러리 추가 없이 Alpine+vanilla+CSS)
- **산출물**: IMPLEMENTATION_SPEC.md **§12 인터랙션·모션 명세** 신설 (전체 코드 포함)
  - 공통: 모션 토큰(--mt-*, --ease-brush), transform/opacity 한정, reduced-motion 일괄 블록(12-10), IO 4개 이하·JS 6KB 예산
  - 전역: 스크롤 리빌+stagger(12-1), 네비 스크롤스파이+읽기 진행바(12-2), 백투탑(12-9) — 셋 다 vanilla라 CDN 폴백에서도 동작
  - Hero: 로고 SVG 스트로크 드로우(붓글씨 그려지는 효과, use 상속 활용), CTA 아이콘 전진, 스크롤 큐
  - 여정: 활성 노드까지 진행선 골드 채움, 3패널 x-show 전환, ←/→ 키보드 순환
  - 핵심기술: CSS grid-rows 아코디언(JS 0줄 개폐 모션), metric 칩 펄스 플래시
  - 실증사례: 호버 오버레이("자세히 보기"), 라이트박스 이전/다음+화살표 키 순회, scale-in
  - 연구성과: 집계 카운트업(문자열 "36+" 파싱), 탭 전환 stagger 재생, 대표성과 골드 스윕
  - 연구진: 카드별 팔레트 액센트 글로우(캐리커처 팔레트 선반영), 복사 버튼 copy→check 모프
- §6 reduced-motion 블록을 §12-10으로 대체 지정, §10 체크리스트에 인터랙션 6항목 추가, §11에 Phase 2.5(인터랙션 패스) 삽입
- CONTENT_SPEC.md §10 신설: 인터랙션 노출 문구 5종 등록
- **다음 단계**: Phase 1 구현 (변동 없음)

### 추가 산출물 (같은 날, 6차 작업) — 비주얼 디자인 상세 설계
- **목표**: 구현 전 단계로, 하위 모델이 시각적 임의 판단 없이 구현 가능하도록 디자인을 수치로 고정 (사용자 지시: 구현 보류, 설계 디테일 우선)
- **산출물**: docs/DESIGN_SPEC.md v1.0 신규 작성
  - §1 디자인 토큰: 컬러 역할 규칙(골드=성과/틸=인터랙션/코랄=진행 중 전용, 조합 금지 규칙), 타이포 9단 스케일, 4pt 스페이싱, 형태·표면
  - §2 반응형 시스템: D/T/M 3단 × 10개 레이아웃 변화 표
  - §3 섹션별 상세 레이아웃: 와이어프레임 + 치수 (Hero/여정/기술/실증/성과/연구진)
  - §4 컴포넌트 상태 매트릭스: 12개 컴포넌트 × default/hover/active, 전역 focus-visible 사양
  - §5 프로필 캐리커처 아트 디렉션: 스타일·구도·액센트 매핑·생성 프롬프트 템플릿·실사진 duotone 대체 규칙
  - §6 OG 이미지(1200×630)·파비콘 사양
  - §7 폴백 모드 시각 사양, §8 디자인 QA 체크리스트
- IMPLEMENTATION_SPEC 헤더에 DESIGN_SPEC 참조 추가 → 명세 4종 체계 완성 (구현/콘텐츠/인터랙션/비주얼)
- **미해결**: 파비콘 단순화 시안(TODO_CONFIRM), 박찬우·백서현 직급, 캐리커처 vs 실사진 결정
- **다음 단계**: Phase 1 구현 착수 (사용자 승인 대기)

## 2026-07-08

### 7차 작업 — Phase 3 사전: 이미지 후보 추출·검증·검토 시트 (비구현 작업)
- **목표**: CONTENT_SPEC 지정 페이지에서 이미지 후보를 추출하고 실물 검증 후 사용자 선별 준비
- **산출물** (전부 세션 스크래치 — 커밋 비대상):
  - PyMuPDF로 3개 PDF의 지정 12개 페이지에서 후보 62건 추출 (전체 렌더 12 + 임베디드 50)
  - 콘택트 시트 3장으로 전 후보 실물 식별 완료
  - 사용자 선별용 검토 시트 `image-review.html` 생성 (슬롯 10개 × 후보, ⭐추천 표시)
- **핵심 발견 (명세 정정 반영)**:
  - MUCH p.61에 5대 서비스 전부의 가로형(16:9 적합) 고해상 화면 존재 → 카드 이미지 1순위 소스를 p.65(세로 모바일)에서 **p.61로 정정** (CONTENT_SPEC §5 갱신)
  - p.65 세로 모바일 캡처는 16:9 크롭 손실 커서 대안 강등, 진묘·동선 p.65본은 저해상(271~328px)으로 비권장 표기
  - ULSOO p.36·p.40·p.42는 임베디드 이미지 없음(벡터/합성) → 선택 시 페이지 렌더 크롭 방식 명시
  - case-metaverse 추천: CHIC p.91 img01 (VR 반가사유상 전시실 — '사유의 방' 서사와 부합)
  - 보너스: MUCH p.61 img05 (전시장 모바일 사용 실사) og-cover 후보로 기록
- **미해결**: 사용자 이미지 선별 (슬롯당 1장), field-ai/field-gen 크롭 진행 여부
- **다음 단계**: 선별 접수 → 크롭·WebP 변환·assets/img 배치 (Phase 3 본작업), 이후 Phase 1 구현 승인 대기

### 8차 작업 — 전체 구현 (Phase 1~3) + 테스트·검증 루프
- **목표**: 명세 4종 기반 전면 구현을 자동 (구현→테스트→검증) 루프로 완료
- **산출물**:
  - `assets/data.js` — §3 데이터 계약 전문 (18KB)
  - `index.html` 전면 개편 — 6섹션(여정 신규), KPI/집계 카운트업, 분야 탭+아코디언, 6카드+라이트박스(‹›/ESC), 연구진 4인, §12 인터랙션 전체(리빌·스파이·진행바·로고 드로우·백투탑), CDN 폴백 §7 동기화
  - `assets/img/` — 검증·크롭·WebP 변환 이미지 9장(471KB) + og-cover(DESIGN_SPEC §6 사양 생성, 24KB) + favicon.svg(공식 로고 원형)
- **검증 결과** (로컬 프리뷰, §10 수용 기준):
  - 구조: 6섹션·네비 5·KPI 4(31,022 콤마)·여정 3노드·기술 3분야/아코디언·6카드(이미지 전부 로드)·성과 4탭 15항목·연구진 4인(이니셜 폴백 동작) 전부 통과
  - 인터랙션: 여정 전환/진행선, 분야 전환, 아코디언 단일 열림, 성과 탭, 라이트박스 열기/다음/ESC 통과
  - 반응형 375px: 가로 오버플로 0, 2×2 그리드, 여정 바 303px 수용
  - 폴백(Alpine 제거 시뮬): 전 콘텐츠 펼침 렌더 정상 (여정 3·기술 14아코디언·성과 15항목)
  - 금지 문구 0건, Ref/ 커밋 배제, 콘솔 에러 0건
- **루프 중 수정한 버그**: 모바일 1열에서 member-avatar 과대 → max-width 15rem 캡
- **스펙 정정 노트**: §12-4의 `data.journeys` 표기는 §3 계약의 `journey`로 통일 구현
- **다음 단계**: git push + GitHub Pages 활성화

### 9차 작업 — GitHub Pages 자동 배포 완료 (프로젝트 목표 달성)
- **목표**: gh CLI 부재 환경에서 Pages 활성화까지 자동 완료
- **방법**: `.github/workflows/deploy-pages.yml` 추가 — `actions/configure-pages@v5`의 `enablement: true`로 첫 실행 시 Pages 자동 활성화, `upload-pages-artifact`+`deploy-pages`로 배포 (커밋 1613a39)
- **검증**:
  - Actions 첫 실행 `completed success`
  - 공개 URL `https://kimheekwon.github.io/ULSOO-Heritage-AI/` → HTTP 200 (74KB), 타이틀 정상
  - 핵심 애셋 4종(data.js·case-banga.webp·og-cover.webp·favicon.svg) 전부 200
- **상태**: 홈페이지 공개 서비스 개시. 이후 main push만으로 자동 재배포됨
- **남은 항목 (사용자 입력 필요)**: ① 연구진 사진/캐리커처 4점 (`assets/members/member-01~04.webp`, 배치 즉시 자동 반영 — 현재 이니셜 아바타) ② 박찬우·백서현 직급 확정 (data.js `role` 필드) ③ 파비콘 단순화 시안 채택 여부

### 10차 작업 — 연구진 직급 확정 반영
- 박찬우: 연구원 → **책임연구원** / 백서현: 연구원 → **박사후연구원(Post-Doc)** (사용자 확정)
- data.js·IMPLEMENTATION_SPEC §3·CONTENT_SPEC §7 동기 갱신, 직급 TODO_CONFIRM 전부 해소. 로컬 프리뷰 렌더 검증 후 push(자동 재배포)
- 남은 사용자 입력: 연구진 사진/캐리커처 4점, 파비콘 단순화 시안 여부

### 11차 작업 — 섹션 간격 축소 + 한/영 언어 토글(영어 버전)
- **목표**: 섹션 간 여백 축소 + 영어 버전 추가 (사용자 요청)
- **간격**: `section` 세로 패딩 6rem→3.5rem, `.section-head` 하단 3rem→2rem (섹션 간 체감 여백 약 42% 축소)
- **i18n 아키텍처**: `assets/data.js`를 `ULSOO_I18N = { ko, en }` 이중 구조로 재작성 (id/key는 언어 공통). 각 언어에 `ui` 객체(섹션 제목·lede·버튼·뱃지·aria 등 26키) 추가
  - Alpine: `lang` 상태 + `get data()/ui()/navItems()` 게터로 반응형 전환. 헤더에 `.lang-toggle` 버튼(EN↔한국어), localStorage(`ulsooLang`) 지속, `<html lang>` 동기
  - 정적 HTML 문구 전부 `x-text="ui.*"` 바인딩(KO 기본값 유지), copyEmail 토스트 언어 분기
  - CDN 폴백도 `ULSOO_I18N[localStorage]` 기반 렌더 + 토글(설정 후 reload) 지원
  - 영문: 검증 수치 보존, ULSOO 항목 "target/development target" 명시, 연구진 로마자 표기(Lee Jae-ho 등)
- **루프 중 수정한 버그 3건**:
  1. KPI/성과 카운트업이 프리뷰 환경에서 IntersectionObserver 초기 콜백 미발화 → 스크롤 위치 기반(getBoundingClientRect + scroll 이벤트) 트리거로 교체
  2. 스크롤 리빌도 동일 IO 이슈로 `.reveal`이 opacity:0에 잔류 → 스크롤 위치 기반 reveal로 교체(실브라우저·프리뷰·폴백 모두 동작)
  3. EN 폴백에서 실증사례 헤더만 미번역(폴백이 카드 그리드만 재구성) → 헤더 h2/lede도 번역 반영
- **검증**: KO/EN 전 섹션 전환·카운트업·리빌·라이트박스·언어 지속(리로드)·EN 폴백(헤더 포함) 전부 통과. 금지 문구 0, 핵심 수치 보존
- **다음 단계**: git push(자동 재배포). 연구진 사진은 여전히 대기

### 12차 작업 — 연구진 프로필 이미지 4점 반영
- **소스**: Ref/{이재호,김희권,박찬우,백서현}.png (조선시대 한복·갓 스타일 캐리커처 4인 세트, RGBA 세로형)
- **처리**: 흰 배경 평탄화 → 얼굴 세로중심 44% 기준 정사각 크롭(가로 전폭) → 512×512 WebP q88 (총 118KB) → assets/members/member-01~04.webp
- **검증**: 4개 파일 200·image/webp 제공 확인, 데스크톱 4열 + 모바일 1열에서 이니셜 폴백 대신 실제 이미지 표시, 백서현 카드 '문화유산보존과학' 태그 동시 확인
- **주의**: Ref/ 원본 PNG는 .gitignore로 커밋 배제(git ls-files grep Ref = 0), WebP 변환본만 커밋
- **결과**: 연구진 4인 프로필 완비 → 남은 선택 항목(파비콘 단순화 시안) 외 주요 콘텐츠 완성

## 2026-07-09

### 13차 작업 — 미리보기 기반 텍스트 정렬 개선 + 실증사례 링크 기능
- **한국어 어절 줄바꿈**: body에 `word-break: keep-all` + `overflow-wrap: break-word` → 전 섹션 본문이 단어 중간에서 끊기던 문제 해소("검증받았습니다"·"다룹니다"·"진단합니다"·"확인할" 등). 데스크톱·모바일 가로 오버플로 0 확인
- **모바일 헤더 정리**: ≤640px에서 로고 부제(긴 영문)가 EN 토글과 56px 겹치던 문제 → 좁은 화면에서 부제 숨김("ULSOO" 워드마크만). 데스크톱은 부제 유지
- **실증사례 바로가기 링크(준비)**: 6개 project에 `link` 필드 추가(현재 전부 빈 값 → 버튼 미표시). 링크가 있는 카드에만 좌하단 "바로 확인하기 ↗"(시안, target=_blank rel=noopener) 자동 표시. ui.caseVisit(한/영) 추가, CDN 폴백에도 반영. 임시 링크로 렌더 검증 후 원복
- **미해결**: 각 사례의 실제 공개 URL(사용자 제공 대기) → 받는 대로 data.js link 채워 재배포
- 검증: 미리보기(1280/375) 텍스트 정렬·헤더·링크 버튼 조건부 표시 전부 통과

### 14차 작업 — 지난 과제(CHIC·MUCH) 홈페이지 링크 연결
- 연구 여정 CHIC/MUCH 카드에 지난 과제 공식 사이트 링크 추가(핵심 연구 설명=기존 subtitle+mission+highlights 바로 아래 배치)
  - CHIC → https://sites.google.com/view/intelligent-curation/home
  - MUCH → https://sites.google.com/view/much0/much
  - ULSOO → 현재 진행 과제(이 사이트)라 링크 없음
- data.js journey에 siteUrl 필드(한/영 공통), ui.journeySite(한 "과제 홈페이지 방문" / 영 "Visit project site") 추가, "과제 홈페이지 방문 ↗"(시안, target=_blank rel=noopener) 조건부 렌더, CDN 폴백에도 반영
- 검증: KO/EN에서 CHIC·MUCH 링크 표시·URL 정확, ULSOO 미표시 확인. 미리보기 통과

## 2026-08-03

### 15차 작업 — "먹과 한지" 스타일 레퍼런스 명세 + 색상톤 예시
- **목표**: 사용자가 전달한 Structured 스타일 가이드(갤러리 미학)를 검토하고, 채택 의견으로 제시한 "한국적 번안" 방향을 동일 형식의 스타일 명세로 문서화 + 색상톤 시각 예시 제작
- **결정사항**: 전면 이식이 아닌 번안 — ① 세리프 디스플레이는 라틴/한글 이중 규칙(라틴 압축, 한글 행간·트래킹 확보) ② 이미지는 유산 원본(분위기)/연구 스크린샷(카드 내부) 이원 규칙 ③ 무채색 원칙에서 금(#d6a452) 단일 악센트만 생존(전통색 teal·red·coral 은퇴) ④ 내비+EN 토글 유지 ⑤ 텍스트 하한 11px
- **산출물**:
  - [[STYLE_REF_Structured]] — 사용자 전달 원본 보관 (docs/)
  - [[STYLE_REF_MeokHanji]] — 번안 명세 v1.0 (팔레트 8토큰·타이포 이중 규칙·컴포넌트·Do/Don't·CSS 변수·원전 대비 이탈 7항). 명암비 전 조합 WCAG 실측 포함 (본문 14.06:1, 뮤트 6.90:1, 다크 악센트 8.17:1, 라이트 악센트 4.77:1 — 전부 AA 이상; Geum on Hanji 1.72:1은 텍스트 금지 명문화)
  - `docs/STYLE_REF_MeokHanji_preview.html` — 색상톤 예시 (스와치 8종·히어로 목업·먹 룸 대시보드·카드/이중 타이포 샘플). 미리보기 렌더 검증 완료
- **현재 진행도**: 스타일 방향 제안·문서화 단계. index.html은 미변경 (리스킨 미착수)
- **남은 미해결**: 이 스타일 채택 여부 및 착수 시점 (사용자 결정 대기). 채택 시 G3 프로토타입(방향 비교) 또는 바로 리스킨 여부 확인 필요
- **다음 단계**: 채택 확정 시 STYLE_REF_MeokHanji를 루트 DESIGN.md로 승격 → 표현 계층 리스킨 (data.js·i18n·검증 수치 계층은 불변)

### 16차 작업 — 한지 질감 + 붓글씨 워드마크 (사용자 피드백 반영)
- **피드백**: 배경은 한지 느낌, ULSOO는 붓으로 쓴 느낌으로
- **질감**: SVG feTurbulence 노이즈 2겹(섬유 입자 0.075 + 가로 발簾 결 0.055, multiply 블렌드) 고정 오버레이 — 외부 이미지 없이 데이터 URI 인라인, 전 섹션 공통. 합계 불투명도 ≤0.15 상한을 명세에 규칙화(명암비 실측치 보호)
- **워드마크**: 세리프 → 붓글씨 서체로 전환, rotate −1.2deg, 네거티브 트래킹 금지(붓 획 간격 보존). 후보 2종 비교 스트립 추가 — A안 Nanum Brush Script(유려한 붓끝, 현 기본값) / B안 East Sea Dokdo(갈필·속도감). 붓 서체는 워드마크 전용으로 제한(세리프=감정, 그로테스크=기능, 붓=서명). 장기 지향점은 폰트가 아닌 실제 붓글씨 SVG 레터링임을 명세에 기록
- **산출물**: `docs/STYLE_REF_MeokHanji_preview.html` 갱신, [[STYLE_REF_MeokHanji]] 개정(Hero Wordmark 재정의, 한지 질감 규칙 신설, 이탈 목록 8·9항 추가). 미리보기 렌더 검증 완료
- **남은 미해결**: 붓 서체 A/B안 선택 (사용자 결정 대기)

### 17차 작업 — 붓서체 B안 확정 · 한지 질감 강화 · 오방색 로고 복원
- **피드백 반영 3건**:
  1. 워드마크 서체 East Sea Dokdo(동해독도체, B안) 확정 — A안 기각, 비교 스트립 제거
  2. 한지 질감 체감 불가 문제 → 노이즈 불투명도 상향(섬유 0.16, 가로 결 0.11). 명세의 합계 상한을 0.15→0.30으로 개정(multiply 노이즈는 명암비 실측 여유 내 안전)
  3. 현행 사이트의 오방색 붓획 SVG 로고를 예시 헤더에 복원(index.html 심볼 이식) — 명세에 Logo Mark 컴포넌트 신설: 로고는 "금 단일 악센트" 원칙의 유일한 예외(은퇴한 전통색은 로고 안에서만 생존), 로고 외 그래픽 마크 금지
- **산출물**: `docs/STYLE_REF_MeokHanji_preview.html` 갱신(헤더 목업: 로고+붓 워드마크+텍스트 내비+EN 토글), [[STYLE_REF_MeokHanji]] 개정(B안 확정·질감 수치·Logo Mark). 미리보기 렌더 검증 완료
- **현재 진행도**: 스타일 방향 수렴 단계 — 팔레트·질감·워드마크·로고 규칙 확정. index.html 리스킨은 미착수(사용자 승인 대기)

### 18차 작업 — 홈페이지 전면 리스킨: '먹과 한지' 스타일 적용
- **목표**: 확정된 [[DESIGN]] 스타일을 실제 index.html에 적용 (다크 네이비 → 한지 라이트 갤러리)
- **원칙**: 표현 계층만 교체 — data.js·i18n·검증 수치·IA 6섹션·반응형 규칙(11·13차 결정)은 불변
- **적용 내역**:
  - 팔레트: `:root` 토큰을 먹·한지 8종으로 교체. 오방색(navy/teal/red/coral)은 **로고 마크 전용**으로 격리 존치
  - 캔버스: 다크 그라디언트 4겹 → 한지 스캔 텍스처(`assets/img/hanji-texture.webp`, multiply 블렌드). heritage-grid 격자·글로우·backdrop-blur·radial 그라디언트 전부 제거(플랫 원칙)
  - 타이포: Noto Serif KR(h1·h2·h3·journey-verb) + East Sea Dokdo(헤더/히어로 ULSOO 워드마크, rotate −1.2deg) 추가. 본문은 Pretendard 유지
  - 컴포넌트: 카드=백자+회백 헤어라인, 버튼=풀 필(먹/설백), 태그=아웃라인, 신호등 점=먹 농담 3단계
  - **먹 룸**: #achievements만 다크 섹션으로 스코프 지정 — 집계 숫자가 금(#d6a452)으로 빛나는 유일한 구간
  - Tailwind 유틸리티 재매핑 확장(기존 !important 패턴 계승): slate/cyan/amber/emerald/red 계열 → 먹·담묵·진금·회백
  - CDN 폴백 렌더러의 인라인 하드코딩 색상 전량 치환, kpiColor() 헬퍼 라이트 팔레트로 교체
  - is-current 밑줄이 붓 워드마크에 걸리던 문제 → `.nav-links a.is-current`로 스코프 축소
- **검증 (2026-08-03 실측, 로컬 서버 1280×900 / 375×812)**: 6섹션 전부 렌더 정상, 한/영 토글 전환 정상, KPI·성과 카운트업 동작, 모바일 가로 오버플로 0, 콘솔 오류 0
  - 미리보기 패널이 백그라운드일 때 rAF가 멈춰 카운터가 중간값에 고정되는 현상은 패널 아티팩트(사이트 결함 아님, 패널 표시 상태에서 31,022·54+·3·30+·24+ 완주 확인)
- **산출물**: [[index]] (315+/222− 변경), `assets/img/hanji-texture.webp`, 루트 [[DESIGN]] 승격
- **남은 미해결**: 커밋·push 미실행(사용자 승인 대기 — push 시 GitHub Pages 자동 재배포). 실증사례 6건 공개 URL은 여전히 대기

## 2026-08-05

### 19차 작업 — CHIC 과제 링크를 리뉴얼 홈페이지로 교체
- **배경**: CHIC 과제 홈페이지가 리뉴얼되어 ETRI-ULSOO/CHIC-Homepage 저장소로 이전(2026-08-05 push). 기존 Google Sites 링크가 구버전을 가리키고 있었음
- **변경**: `assets/data.js` journey[chic].siteUrl 한/영 2곳
  - 구: https://sites.google.com/view/intelligent-curation/home
  - 신: https://etri-ulsoo.github.io/CHIC-Homepage/ (GitHub Pages API로 실제 배포 URL 확인, 브라우저 접속 검증)
- **영향 범위**: data.js 단일 원천이므로 Alpine 렌더·CDN 폴백 양쪽에 자동 반영 (index.html 변경 없음). MUCH·ULSOO 링크는 불변
- **검증(2026-08-05 실측)**: 로컬 서버에서 CHIC 탭 선택 시 링크 렌더 확인 — KO "과제 홈페이지 방문" / EN "Visit project site", href 정확, rel=noopener noreferrer, 콘솔 오류 0
- **참고**: 여정 패널은 선택된 과제 1건만 렌더되므로 기본 상태(ULSOO, siteUrl 없음)에서는 링크가 보이지 않는 것이 정상 동작
